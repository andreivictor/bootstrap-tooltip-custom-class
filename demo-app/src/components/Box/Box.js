import React from 'react';
import PropTypes from 'prop-types';
import './Box.scss';

const Box = () => (
  <div className="Box" data-testid="Box">
    Box Component
  </div>
);

Box.propTypes = {};

Box.defaultProps = {};

export default Box;
